<!DOCTYPE html>
<html>
  <head>
    <title>Codebreaker</title>
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css" type="text/css">
    <link rel="stylesheet" href="/stylesheets/application.css" type="text/css">    
  </head>
  <body>
    <div id="container">
    <h1>Codebreaker statistic</h1>
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
          <table class="table">
            <thead>
              <tr>
                <th>#</th>
                <th>User</th>
                <th>Attempts</th>
                <th>Result</th>
              </tr>
            </thead>
            <tbody>
            <% unless show_score.empty?  %>
              <% show_score.each_with_index do |item, index|%>
                <tr>
                  <td><%= index + 1 %></td>
                  <td><%= item[0] %></td>
                  <td><%= item[1] %></td>
                  <td><%= item[2] %></td>
                </tr>
              <% end %>
            <% end %>
            </tbody>
          </table>
        </div>
       </div>
       <a href = "/play_again" class="btn">Return to Game</a>
    </div>
  </body>
</html>